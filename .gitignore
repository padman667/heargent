# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# macOS
.DS_Store

# Tooling
.claude/

# Secrets / local environment (never commit API keys)
.env
.env.*
*.key

# Local repo copies / backups — these contain old Claude session transcripts
# with plaintext API keys; never commit them.
heargent_otherlaptop/
heargent_old/
*.jsonl
